    // Домашнее задание:
    // 1. Создать обобщенный BaseDao<T, ..> класс, позволяющий выполнять
    // операции сохранение объектов в БД и получения их по ID для любой сущностью
    // Пример:
    // BaseDao<Student, ..> studentDao = new BaseDao<>();
    // Student s = studentDao.get(1L);
    // Взаимодействие с БД организуете через Hibernate
